// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NeoPlayful/maple-gateway/server/ent/bluegreenevent"
	"github.com/NeoPlayful/maple-gateway/server/ent/predicate"
	"github.com/google/uuid"
)

// BluegreenEventQuery is the builder for querying BluegreenEvent entities.
type BluegreenEventQuery struct {
	config
	ctx        *QueryContext
	order      []bluegreenevent.OrderOption
	inters     []Interceptor
	predicates []predicate.BluegreenEvent
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the BluegreenEventQuery builder.
func (beq *BluegreenEventQuery) Where(ps ...predicate.BluegreenEvent) *BluegreenEventQuery {
	beq.predicates = append(beq.predicates, ps...)
	return beq
}

// Limit the number of records to be returned by this query.
func (beq *BluegreenEventQuery) Limit(limit int) *BluegreenEventQuery {
	beq.ctx.Limit = &limit
	return beq
}

// Offset to start from.
func (beq *BluegreenEventQuery) Offset(offset int) *BluegreenEventQuery {
	beq.ctx.Offset = &offset
	return beq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (beq *BluegreenEventQuery) Unique(unique bool) *BluegreenEventQuery {
	beq.ctx.Unique = &unique
	return beq
}

// Order specifies how the records should be ordered.
func (beq *BluegreenEventQuery) Order(o ...bluegreenevent.OrderOption) *BluegreenEventQuery {
	beq.order = append(beq.order, o...)
	return beq
}

// First returns the first BluegreenEvent entity from the query.
// Returns a *NotFoundError when no BluegreenEvent was found.
func (beq *BluegreenEventQuery) First(ctx context.Context) (*BluegreenEvent, error) {
	nodes, err := beq.Limit(1).All(setContextOp(ctx, beq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{bluegreenevent.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (beq *BluegreenEventQuery) FirstX(ctx context.Context) *BluegreenEvent {
	node, err := beq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first BluegreenEvent ID from the query.
// Returns a *NotFoundError when no BluegreenEvent ID was found.
func (beq *BluegreenEventQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = beq.Limit(1).IDs(setContextOp(ctx, beq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{bluegreenevent.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (beq *BluegreenEventQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := beq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single BluegreenEvent entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one BluegreenEvent entity is found.
// Returns a *NotFoundError when no BluegreenEvent entities are found.
func (beq *BluegreenEventQuery) Only(ctx context.Context) (*BluegreenEvent, error) {
	nodes, err := beq.Limit(2).All(setContextOp(ctx, beq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{bluegreenevent.Label}
	default:
		return nil, &NotSingularError{bluegreenevent.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (beq *BluegreenEventQuery) OnlyX(ctx context.Context) *BluegreenEvent {
	node, err := beq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only BluegreenEvent ID in the query.
// Returns a *NotSingularError when more than one BluegreenEvent ID is found.
// Returns a *NotFoundError when no entities are found.
func (beq *BluegreenEventQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = beq.Limit(2).IDs(setContextOp(ctx, beq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{bluegreenevent.Label}
	default:
		err = &NotSingularError{bluegreenevent.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (beq *BluegreenEventQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := beq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of BluegreenEvents.
func (beq *BluegreenEventQuery) All(ctx context.Context) ([]*BluegreenEvent, error) {
	ctx = setContextOp(ctx, beq.ctx, ent.OpQueryAll)
	if err := beq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*BluegreenEvent, *BluegreenEventQuery]()
	return withInterceptors[[]*BluegreenEvent](ctx, beq, qr, beq.inters)
}

// AllX is like All, but panics if an error occurs.
func (beq *BluegreenEventQuery) AllX(ctx context.Context) []*BluegreenEvent {
	nodes, err := beq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of BluegreenEvent IDs.
func (beq *BluegreenEventQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if beq.ctx.Unique == nil && beq.path != nil {
		beq.Unique(true)
	}
	ctx = setContextOp(ctx, beq.ctx, ent.OpQueryIDs)
	if err = beq.Select(bluegreenevent.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (beq *BluegreenEventQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := beq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (beq *BluegreenEventQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, beq.ctx, ent.OpQueryCount)
	if err := beq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, beq, querierCount[*BluegreenEventQuery](), beq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (beq *BluegreenEventQuery) CountX(ctx context.Context) int {
	count, err := beq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (beq *BluegreenEventQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, beq.ctx, ent.OpQueryExist)
	switch _, err := beq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (beq *BluegreenEventQuery) ExistX(ctx context.Context) bool {
	exist, err := beq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the BluegreenEventQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (beq *BluegreenEventQuery) Clone() *BluegreenEventQuery {
	if beq == nil {
		return nil
	}
	return &BluegreenEventQuery{
		config:     beq.config,
		ctx:        beq.ctx.Clone(),
		order:      append([]bluegreenevent.OrderOption{}, beq.order...),
		inters:     append([]Interceptor{}, beq.inters...),
		predicates: append([]predicate.BluegreenEvent{}, beq.predicates...),
		// clone intermediate query.
		sql:  beq.sql.Clone(),
		path: beq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		BgID uuid.UUID `json:"bg_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.BluegreenEvent.Query().
//		GroupBy(bluegreenevent.FieldBgID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (beq *BluegreenEventQuery) GroupBy(field string, fields ...string) *BluegreenEventGroupBy {
	beq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &BluegreenEventGroupBy{build: beq}
	grbuild.flds = &beq.ctx.Fields
	grbuild.label = bluegreenevent.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		BgID uuid.UUID `json:"bg_id,omitempty"`
//	}
//
//	client.BluegreenEvent.Query().
//		Select(bluegreenevent.FieldBgID).
//		Scan(ctx, &v)
func (beq *BluegreenEventQuery) Select(fields ...string) *BluegreenEventSelect {
	beq.ctx.Fields = append(beq.ctx.Fields, fields...)
	sbuild := &BluegreenEventSelect{BluegreenEventQuery: beq}
	sbuild.label = bluegreenevent.Label
	sbuild.flds, sbuild.scan = &beq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a BluegreenEventSelect configured with the given aggregations.
func (beq *BluegreenEventQuery) Aggregate(fns ...AggregateFunc) *BluegreenEventSelect {
	return beq.Select().Aggregate(fns...)
}

func (beq *BluegreenEventQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range beq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, beq); err != nil {
				return err
			}
		}
	}
	for _, f := range beq.ctx.Fields {
		if !bluegreenevent.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if beq.path != nil {
		prev, err := beq.path(ctx)
		if err != nil {
			return err
		}
		beq.sql = prev
	}
	return nil
}

func (beq *BluegreenEventQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*BluegreenEvent, error) {
	var (
		nodes = []*BluegreenEvent{}
		_spec = beq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*BluegreenEvent).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &BluegreenEvent{config: beq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, beq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (beq *BluegreenEventQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := beq.querySpec()
	_spec.Node.Columns = beq.ctx.Fields
	if len(beq.ctx.Fields) > 0 {
		_spec.Unique = beq.ctx.Unique != nil && *beq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, beq.driver, _spec)
}

func (beq *BluegreenEventQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(bluegreenevent.Table, bluegreenevent.Columns, sqlgraph.NewFieldSpec(bluegreenevent.FieldID, field.TypeUUID))
	_spec.From = beq.sql
	if unique := beq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if beq.path != nil {
		_spec.Unique = true
	}
	if fields := beq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, bluegreenevent.FieldID)
		for i := range fields {
			if fields[i] != bluegreenevent.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := beq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := beq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := beq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := beq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (beq *BluegreenEventQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(beq.driver.Dialect())
	t1 := builder.Table(bluegreenevent.Table)
	columns := beq.ctx.Fields
	if len(columns) == 0 {
		columns = bluegreenevent.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if beq.sql != nil {
		selector = beq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if beq.ctx.Unique != nil && *beq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range beq.predicates {
		p(selector)
	}
	for _, p := range beq.order {
		p(selector)
	}
	if offset := beq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := beq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// BluegreenEventGroupBy is the group-by builder for BluegreenEvent entities.
type BluegreenEventGroupBy struct {
	selector
	build *BluegreenEventQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (begb *BluegreenEventGroupBy) Aggregate(fns ...AggregateFunc) *BluegreenEventGroupBy {
	begb.fns = append(begb.fns, fns...)
	return begb
}

// Scan applies the selector query and scans the result into the given value.
func (begb *BluegreenEventGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, begb.build.ctx, ent.OpQueryGroupBy)
	if err := begb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BluegreenEventQuery, *BluegreenEventGroupBy](ctx, begb.build, begb, begb.build.inters, v)
}

func (begb *BluegreenEventGroupBy) sqlScan(ctx context.Context, root *BluegreenEventQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(begb.fns))
	for _, fn := range begb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*begb.flds)+len(begb.fns))
		for _, f := range *begb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*begb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := begb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// BluegreenEventSelect is the builder for selecting fields of BluegreenEvent entities.
type BluegreenEventSelect struct {
	*BluegreenEventQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (bes *BluegreenEventSelect) Aggregate(fns ...AggregateFunc) *BluegreenEventSelect {
	bes.fns = append(bes.fns, fns...)
	return bes
}

// Scan applies the selector query and scans the result into the given value.
func (bes *BluegreenEventSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, bes.ctx, ent.OpQuerySelect)
	if err := bes.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*BluegreenEventQuery, *BluegreenEventSelect](ctx, bes.BluegreenEventQuery, bes, bes.inters, v)
}

func (bes *BluegreenEventSelect) sqlScan(ctx context.Context, root *BluegreenEventQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(bes.fns))
	for _, fn := range bes.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*bes.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := bes.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
