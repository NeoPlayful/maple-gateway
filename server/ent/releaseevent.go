// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/NeoPlayful/maple-gateway/server/ent/releaseevent"
	"github.com/google/uuid"
)

// ReleaseEvent is the model entity for the ReleaseEvent schema.
type ReleaseEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ReleaseID holds the value of the "release_id" field.
	ReleaseID uuid.UUID `json:"release_id,omitempty"`
	// Action holds the value of the "action" field.
	Action string `json:"action,omitempty"`
	// FromWeight holds the value of the "from_weight" field.
	FromWeight *int `json:"from_weight,omitempty"`
	// ToWeight holds the value of the "to_weight" field.
	ToWeight *int `json:"to_weight,omitempty"`
	// FromVersion holds the value of the "from_version" field.
	FromVersion *uuid.UUID `json:"from_version,omitempty"`
	// ToVersion holds the value of the "to_version" field.
	ToVersion *uuid.UUID `json:"to_version,omitempty"`
	// Detail holds the value of the "detail" field.
	Detail string `json:"detail,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ReleaseEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case releaseevent.FieldFromVersion, releaseevent.FieldToVersion:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case releaseevent.FieldFromWeight, releaseevent.FieldToWeight:
			values[i] = new(sql.NullInt64)
		case releaseevent.FieldAction, releaseevent.FieldDetail:
			values[i] = new(sql.NullString)
		case releaseevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case releaseevent.FieldID, releaseevent.FieldReleaseID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ReleaseEvent fields.
func (re *ReleaseEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case releaseevent.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				re.ID = *value
			}
		case releaseevent.FieldReleaseID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field release_id", values[i])
			} else if value != nil {
				re.ReleaseID = *value
			}
		case releaseevent.FieldAction:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field action", values[i])
			} else if value.Valid {
				re.Action = value.String
			}
		case releaseevent.FieldFromWeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field from_weight", values[i])
			} else if value.Valid {
				re.FromWeight = new(int)
				*re.FromWeight = int(value.Int64)
			}
		case releaseevent.FieldToWeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field to_weight", values[i])
			} else if value.Valid {
				re.ToWeight = new(int)
				*re.ToWeight = int(value.Int64)
			}
		case releaseevent.FieldFromVersion:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field from_version", values[i])
			} else if value.Valid {
				re.FromVersion = new(uuid.UUID)
				*re.FromVersion = *value.S.(*uuid.UUID)
			}
		case releaseevent.FieldToVersion:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field to_version", values[i])
			} else if value.Valid {
				re.ToVersion = new(uuid.UUID)
				*re.ToVersion = *value.S.(*uuid.UUID)
			}
		case releaseevent.FieldDetail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field detail", values[i])
			} else if value.Valid {
				re.Detail = value.String
			}
		case releaseevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				re.CreatedAt = value.Time
			}
		default:
			re.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ReleaseEvent.
// This includes values selected through modifiers, order, etc.
func (re *ReleaseEvent) Value(name string) (ent.Value, error) {
	return re.selectValues.Get(name)
}

// Update returns a builder for updating this ReleaseEvent.
// Note that you need to call ReleaseEvent.Unwrap() before calling this method if this ReleaseEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (re *ReleaseEvent) Update() *ReleaseEventUpdateOne {
	return NewReleaseEventClient(re.config).UpdateOne(re)
}

// Unwrap unwraps the ReleaseEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (re *ReleaseEvent) Unwrap() *ReleaseEvent {
	_tx, ok := re.config.driver.(*txDriver)
	if !ok {
		panic("ent: ReleaseEvent is not a transactional entity")
	}
	re.config.driver = _tx.drv
	return re
}

// String implements the fmt.Stringer.
func (re *ReleaseEvent) String() string {
	var builder strings.Builder
	builder.WriteString("ReleaseEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", re.ID))
	builder.WriteString("release_id=")
	builder.WriteString(fmt.Sprintf("%v", re.ReleaseID))
	builder.WriteString(", ")
	builder.WriteString("action=")
	builder.WriteString(re.Action)
	builder.WriteString(", ")
	if v := re.FromWeight; v != nil {
		builder.WriteString("from_weight=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := re.ToWeight; v != nil {
		builder.WriteString("to_weight=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := re.FromVersion; v != nil {
		builder.WriteString("from_version=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := re.ToVersion; v != nil {
		builder.WriteString("to_version=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("detail=")
	builder.WriteString(re.Detail)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(re.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ReleaseEvents is a parsable slice of ReleaseEvent.
type ReleaseEvents []*ReleaseEvent
