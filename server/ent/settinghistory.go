// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/NeoPlayful/maple-gateway/server/ent/settinghistory"
	"github.com/google/uuid"
)

// SettingHistory is the model entity for the SettingHistory schema.
type SettingHistory struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Section holds the value of the "section" field.
	Section string `json:"section,omitempty"`
	// Key holds the value of the "key" field.
	Key string `json:"key,omitempty"`
	// Version holds the value of the "version" field.
	Version int `json:"version,omitempty"`
	// Value holds the value of the "value" field.
	Value json.RawMessage `json:"value,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SettingHistory) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case settinghistory.FieldValue:
			values[i] = new([]byte)
		case settinghistory.FieldVersion:
			values[i] = new(sql.NullInt64)
		case settinghistory.FieldSection, settinghistory.FieldKey:
			values[i] = new(sql.NullString)
		case settinghistory.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case settinghistory.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SettingHistory fields.
func (sh *SettingHistory) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case settinghistory.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				sh.ID = *value
			}
		case settinghistory.FieldSection:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field section", values[i])
			} else if value.Valid {
				sh.Section = value.String
			}
		case settinghistory.FieldKey:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field key", values[i])
			} else if value.Valid {
				sh.Key = value.String
			}
		case settinghistory.FieldVersion:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field version", values[i])
			} else if value.Valid {
				sh.Version = int(value.Int64)
			}
		case settinghistory.FieldValue:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field value", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &sh.Value); err != nil {
					return fmt.Errorf("unmarshal field value: %w", err)
				}
			}
		case settinghistory.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				sh.CreatedAt = value.Time
			}
		default:
			sh.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// GetValue returns the ent.Value that was dynamically selected and assigned to the SettingHistory.
// This includes values selected through modifiers, order, etc.
func (sh *SettingHistory) GetValue(name string) (ent.Value, error) {
	return sh.selectValues.Get(name)
}

// Update returns a builder for updating this SettingHistory.
// Note that you need to call SettingHistory.Unwrap() before calling this method if this SettingHistory
// was returned from a transaction, and the transaction was committed or rolled back.
func (sh *SettingHistory) Update() *SettingHistoryUpdateOne {
	return NewSettingHistoryClient(sh.config).UpdateOne(sh)
}

// Unwrap unwraps the SettingHistory entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sh *SettingHistory) Unwrap() *SettingHistory {
	_tx, ok := sh.config.driver.(*txDriver)
	if !ok {
		panic("ent: SettingHistory is not a transactional entity")
	}
	sh.config.driver = _tx.drv
	return sh
}

// String implements the fmt.Stringer.
func (sh *SettingHistory) String() string {
	var builder strings.Builder
	builder.WriteString("SettingHistory(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sh.ID))
	builder.WriteString("section=")
	builder.WriteString(sh.Section)
	builder.WriteString(", ")
	builder.WriteString("key=")
	builder.WriteString(sh.Key)
	builder.WriteString(", ")
	builder.WriteString("version=")
	builder.WriteString(fmt.Sprintf("%v", sh.Version))
	builder.WriteString(", ")
	builder.WriteString("value=")
	builder.WriteString(fmt.Sprintf("%v", sh.Value))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(sh.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SettingHistories is a parsable slice of SettingHistory.
type SettingHistories []*SettingHistory
