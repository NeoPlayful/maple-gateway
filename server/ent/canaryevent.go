// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/NeoPlayful/maple-gateway/server/ent/canaryevent"
	"github.com/google/uuid"
)

// CanaryEvent is the model entity for the CanaryEvent schema.
type CanaryEvent struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ReleaseID holds the value of the "release_id" field.
	ReleaseID uuid.UUID `json:"release_id,omitempty"`
	// Phase holds the value of the "phase" field.
	Phase string `json:"phase,omitempty"`
	// FromWeight holds the value of the "from_weight" field.
	FromWeight *int `json:"from_weight,omitempty"`
	// ToWeight holds the value of the "to_weight" field.
	ToWeight *int `json:"to_weight,omitempty"`
	// Detail holds the value of the "detail" field.
	Detail string `json:"detail,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt    time.Time `json:"created_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CanaryEvent) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case canaryevent.FieldFromWeight, canaryevent.FieldToWeight:
			values[i] = new(sql.NullInt64)
		case canaryevent.FieldPhase, canaryevent.FieldDetail:
			values[i] = new(sql.NullString)
		case canaryevent.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case canaryevent.FieldID, canaryevent.FieldReleaseID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CanaryEvent fields.
func (ce *CanaryEvent) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case canaryevent.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ce.ID = *value
			}
		case canaryevent.FieldReleaseID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field release_id", values[i])
			} else if value != nil {
				ce.ReleaseID = *value
			}
		case canaryevent.FieldPhase:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field phase", values[i])
			} else if value.Valid {
				ce.Phase = value.String
			}
		case canaryevent.FieldFromWeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field from_weight", values[i])
			} else if value.Valid {
				ce.FromWeight = new(int)
				*ce.FromWeight = int(value.Int64)
			}
		case canaryevent.FieldToWeight:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field to_weight", values[i])
			} else if value.Valid {
				ce.ToWeight = new(int)
				*ce.ToWeight = int(value.Int64)
			}
		case canaryevent.FieldDetail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field detail", values[i])
			} else if value.Valid {
				ce.Detail = value.String
			}
		case canaryevent.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ce.CreatedAt = value.Time
			}
		default:
			ce.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CanaryEvent.
// This includes values selected through modifiers, order, etc.
func (ce *CanaryEvent) Value(name string) (ent.Value, error) {
	return ce.selectValues.Get(name)
}

// Update returns a builder for updating this CanaryEvent.
// Note that you need to call CanaryEvent.Unwrap() before calling this method if this CanaryEvent
// was returned from a transaction, and the transaction was committed or rolled back.
func (ce *CanaryEvent) Update() *CanaryEventUpdateOne {
	return NewCanaryEventClient(ce.config).UpdateOne(ce)
}

// Unwrap unwraps the CanaryEvent entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ce *CanaryEvent) Unwrap() *CanaryEvent {
	_tx, ok := ce.config.driver.(*txDriver)
	if !ok {
		panic("ent: CanaryEvent is not a transactional entity")
	}
	ce.config.driver = _tx.drv
	return ce
}

// String implements the fmt.Stringer.
func (ce *CanaryEvent) String() string {
	var builder strings.Builder
	builder.WriteString("CanaryEvent(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ce.ID))
	builder.WriteString("release_id=")
	builder.WriteString(fmt.Sprintf("%v", ce.ReleaseID))
	builder.WriteString(", ")
	builder.WriteString("phase=")
	builder.WriteString(ce.Phase)
	builder.WriteString(", ")
	if v := ce.FromWeight; v != nil {
		builder.WriteString("from_weight=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := ce.ToWeight; v != nil {
		builder.WriteString("to_weight=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("detail=")
	builder.WriteString(ce.Detail)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ce.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CanaryEvents is a parsable slice of CanaryEvent.
type CanaryEvents []*CanaryEvent
