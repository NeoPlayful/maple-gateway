// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/NeoPlayful/maple-gateway/server/ent/certificateoperation"
	"github.com/NeoPlayful/maple-gateway/server/ent/predicate"
	"github.com/google/uuid"
)

// CertificateOperationQuery is the builder for querying CertificateOperation entities.
type CertificateOperationQuery struct {
	config
	ctx        *QueryContext
	order      []certificateoperation.OrderOption
	inters     []Interceptor
	predicates []predicate.CertificateOperation
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CertificateOperationQuery builder.
func (coq *CertificateOperationQuery) Where(ps ...predicate.CertificateOperation) *CertificateOperationQuery {
	coq.predicates = append(coq.predicates, ps...)
	return coq
}

// Limit the number of records to be returned by this query.
func (coq *CertificateOperationQuery) Limit(limit int) *CertificateOperationQuery {
	coq.ctx.Limit = &limit
	return coq
}

// Offset to start from.
func (coq *CertificateOperationQuery) Offset(offset int) *CertificateOperationQuery {
	coq.ctx.Offset = &offset
	return coq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (coq *CertificateOperationQuery) Unique(unique bool) *CertificateOperationQuery {
	coq.ctx.Unique = &unique
	return coq
}

// Order specifies how the records should be ordered.
func (coq *CertificateOperationQuery) Order(o ...certificateoperation.OrderOption) *CertificateOperationQuery {
	coq.order = append(coq.order, o...)
	return coq
}

// First returns the first CertificateOperation entity from the query.
// Returns a *NotFoundError when no CertificateOperation was found.
func (coq *CertificateOperationQuery) First(ctx context.Context) (*CertificateOperation, error) {
	nodes, err := coq.Limit(1).All(setContextOp(ctx, coq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{certificateoperation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (coq *CertificateOperationQuery) FirstX(ctx context.Context) *CertificateOperation {
	node, err := coq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CertificateOperation ID from the query.
// Returns a *NotFoundError when no CertificateOperation ID was found.
func (coq *CertificateOperationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = coq.Limit(1).IDs(setContextOp(ctx, coq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{certificateoperation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (coq *CertificateOperationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := coq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CertificateOperation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CertificateOperation entity is found.
// Returns a *NotFoundError when no CertificateOperation entities are found.
func (coq *CertificateOperationQuery) Only(ctx context.Context) (*CertificateOperation, error) {
	nodes, err := coq.Limit(2).All(setContextOp(ctx, coq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{certificateoperation.Label}
	default:
		return nil, &NotSingularError{certificateoperation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (coq *CertificateOperationQuery) OnlyX(ctx context.Context) *CertificateOperation {
	node, err := coq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CertificateOperation ID in the query.
// Returns a *NotSingularError when more than one CertificateOperation ID is found.
// Returns a *NotFoundError when no entities are found.
func (coq *CertificateOperationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = coq.Limit(2).IDs(setContextOp(ctx, coq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{certificateoperation.Label}
	default:
		err = &NotSingularError{certificateoperation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (coq *CertificateOperationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := coq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CertificateOperations.
func (coq *CertificateOperationQuery) All(ctx context.Context) ([]*CertificateOperation, error) {
	ctx = setContextOp(ctx, coq.ctx, ent.OpQueryAll)
	if err := coq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CertificateOperation, *CertificateOperationQuery]()
	return withInterceptors[[]*CertificateOperation](ctx, coq, qr, coq.inters)
}

// AllX is like All, but panics if an error occurs.
func (coq *CertificateOperationQuery) AllX(ctx context.Context) []*CertificateOperation {
	nodes, err := coq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CertificateOperation IDs.
func (coq *CertificateOperationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if coq.ctx.Unique == nil && coq.path != nil {
		coq.Unique(true)
	}
	ctx = setContextOp(ctx, coq.ctx, ent.OpQueryIDs)
	if err = coq.Select(certificateoperation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (coq *CertificateOperationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := coq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (coq *CertificateOperationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, coq.ctx, ent.OpQueryCount)
	if err := coq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, coq, querierCount[*CertificateOperationQuery](), coq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (coq *CertificateOperationQuery) CountX(ctx context.Context) int {
	count, err := coq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (coq *CertificateOperationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, coq.ctx, ent.OpQueryExist)
	switch _, err := coq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (coq *CertificateOperationQuery) ExistX(ctx context.Context) bool {
	exist, err := coq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CertificateOperationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (coq *CertificateOperationQuery) Clone() *CertificateOperationQuery {
	if coq == nil {
		return nil
	}
	return &CertificateOperationQuery{
		config:     coq.config,
		ctx:        coq.ctx.Clone(),
		order:      append([]certificateoperation.OrderOption{}, coq.order...),
		inters:     append([]Interceptor{}, coq.inters...),
		predicates: append([]predicate.CertificateOperation{}, coq.predicates...),
		// clone intermediate query.
		sql:  coq.sql.Clone(),
		path: coq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Hostname string `json:"hostname,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CertificateOperation.Query().
//		GroupBy(certificateoperation.FieldHostname).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (coq *CertificateOperationQuery) GroupBy(field string, fields ...string) *CertificateOperationGroupBy {
	coq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CertificateOperationGroupBy{build: coq}
	grbuild.flds = &coq.ctx.Fields
	grbuild.label = certificateoperation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Hostname string `json:"hostname,omitempty"`
//	}
//
//	client.CertificateOperation.Query().
//		Select(certificateoperation.FieldHostname).
//		Scan(ctx, &v)
func (coq *CertificateOperationQuery) Select(fields ...string) *CertificateOperationSelect {
	coq.ctx.Fields = append(coq.ctx.Fields, fields...)
	sbuild := &CertificateOperationSelect{CertificateOperationQuery: coq}
	sbuild.label = certificateoperation.Label
	sbuild.flds, sbuild.scan = &coq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CertificateOperationSelect configured with the given aggregations.
func (coq *CertificateOperationQuery) Aggregate(fns ...AggregateFunc) *CertificateOperationSelect {
	return coq.Select().Aggregate(fns...)
}

func (coq *CertificateOperationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range coq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, coq); err != nil {
				return err
			}
		}
	}
	for _, f := range coq.ctx.Fields {
		if !certificateoperation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if coq.path != nil {
		prev, err := coq.path(ctx)
		if err != nil {
			return err
		}
		coq.sql = prev
	}
	return nil
}

func (coq *CertificateOperationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CertificateOperation, error) {
	var (
		nodes = []*CertificateOperation{}
		_spec = coq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CertificateOperation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CertificateOperation{config: coq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, coq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (coq *CertificateOperationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := coq.querySpec()
	_spec.Node.Columns = coq.ctx.Fields
	if len(coq.ctx.Fields) > 0 {
		_spec.Unique = coq.ctx.Unique != nil && *coq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, coq.driver, _spec)
}

func (coq *CertificateOperationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(certificateoperation.Table, certificateoperation.Columns, sqlgraph.NewFieldSpec(certificateoperation.FieldID, field.TypeUUID))
	_spec.From = coq.sql
	if unique := coq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if coq.path != nil {
		_spec.Unique = true
	}
	if fields := coq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, certificateoperation.FieldID)
		for i := range fields {
			if fields[i] != certificateoperation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := coq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := coq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := coq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := coq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (coq *CertificateOperationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(coq.driver.Dialect())
	t1 := builder.Table(certificateoperation.Table)
	columns := coq.ctx.Fields
	if len(columns) == 0 {
		columns = certificateoperation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if coq.sql != nil {
		selector = coq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if coq.ctx.Unique != nil && *coq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range coq.predicates {
		p(selector)
	}
	for _, p := range coq.order {
		p(selector)
	}
	if offset := coq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := coq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CertificateOperationGroupBy is the group-by builder for CertificateOperation entities.
type CertificateOperationGroupBy struct {
	selector
	build *CertificateOperationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cogb *CertificateOperationGroupBy) Aggregate(fns ...AggregateFunc) *CertificateOperationGroupBy {
	cogb.fns = append(cogb.fns, fns...)
	return cogb
}

// Scan applies the selector query and scans the result into the given value.
func (cogb *CertificateOperationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cogb.build.ctx, ent.OpQueryGroupBy)
	if err := cogb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CertificateOperationQuery, *CertificateOperationGroupBy](ctx, cogb.build, cogb, cogb.build.inters, v)
}

func (cogb *CertificateOperationGroupBy) sqlScan(ctx context.Context, root *CertificateOperationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cogb.fns))
	for _, fn := range cogb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cogb.flds)+len(cogb.fns))
		for _, f := range *cogb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cogb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cogb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CertificateOperationSelect is the builder for selecting fields of CertificateOperation entities.
type CertificateOperationSelect struct {
	*CertificateOperationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cos *CertificateOperationSelect) Aggregate(fns ...AggregateFunc) *CertificateOperationSelect {
	cos.fns = append(cos.fns, fns...)
	return cos
}

// Scan applies the selector query and scans the result into the given value.
func (cos *CertificateOperationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cos.ctx, ent.OpQuerySelect)
	if err := cos.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CertificateOperationQuery, *CertificateOperationSelect](ctx, cos.CertificateOperationQuery, cos, cos.inters, v)
}

func (cos *CertificateOperationSelect) sqlScan(ctx context.Context, root *CertificateOperationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cos.fns))
	for _, fn := range cos.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cos.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cos.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
