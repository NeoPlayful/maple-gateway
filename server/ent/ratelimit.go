// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/NeoPlayful/maple-gateway/server/ent/ratelimit"
	"github.com/google/uuid"
)

// RateLimit is the model entity for the RateLimit schema.
type RateLimit struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// Scope holds the value of the "scope" field.
	Scope string `json:"scope,omitempty"`
	// TenantID holds the value of the "tenant_id" field.
	TenantID *uuid.UUID `json:"tenant_id,omitempty"`
	// DomainID holds the value of the "domain_id" field.
	DomainID *uuid.UUID `json:"domain_id,omitempty"`
	// ServiceID holds the value of the "service_id" field.
	ServiceID *uuid.UUID `json:"service_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Limit holds the value of the "limit" field.
	Limit int `json:"limit,omitempty"`
	// WindowSeconds holds the value of the "window_seconds" field.
	WindowSeconds int `json:"window_seconds,omitempty"`
	// Burst holds the value of the "burst" field.
	Burst int `json:"burst,omitempty"`
	// ResponseCode holds the value of the "response_code" field.
	ResponseCode int `json:"response_code,omitempty"`
	// Status holds the value of the "status" field.
	Status string `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RateLimit) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ratelimit.FieldTenantID, ratelimit.FieldDomainID, ratelimit.FieldServiceID:
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case ratelimit.FieldLimit, ratelimit.FieldWindowSeconds, ratelimit.FieldBurst, ratelimit.FieldResponseCode:
			values[i] = new(sql.NullInt64)
		case ratelimit.FieldScope, ratelimit.FieldName, ratelimit.FieldStatus:
			values[i] = new(sql.NullString)
		case ratelimit.FieldCreatedAt, ratelimit.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case ratelimit.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RateLimit fields.
func (rl *RateLimit) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ratelimit.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				rl.ID = *value
			}
		case ratelimit.FieldScope:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scope", values[i])
			} else if value.Valid {
				rl.Scope = value.String
			}
		case ratelimit.FieldTenantID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field tenant_id", values[i])
			} else if value.Valid {
				rl.TenantID = new(uuid.UUID)
				*rl.TenantID = *value.S.(*uuid.UUID)
			}
		case ratelimit.FieldDomainID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field domain_id", values[i])
			} else if value.Valid {
				rl.DomainID = new(uuid.UUID)
				*rl.DomainID = *value.S.(*uuid.UUID)
			}
		case ratelimit.FieldServiceID:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field service_id", values[i])
			} else if value.Valid {
				rl.ServiceID = new(uuid.UUID)
				*rl.ServiceID = *value.S.(*uuid.UUID)
			}
		case ratelimit.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				rl.Name = value.String
			}
		case ratelimit.FieldLimit:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field limit", values[i])
			} else if value.Valid {
				rl.Limit = int(value.Int64)
			}
		case ratelimit.FieldWindowSeconds:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field window_seconds", values[i])
			} else if value.Valid {
				rl.WindowSeconds = int(value.Int64)
			}
		case ratelimit.FieldBurst:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field burst", values[i])
			} else if value.Valid {
				rl.Burst = int(value.Int64)
			}
		case ratelimit.FieldResponseCode:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field response_code", values[i])
			} else if value.Valid {
				rl.ResponseCode = int(value.Int64)
			}
		case ratelimit.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				rl.Status = value.String
			}
		case ratelimit.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				rl.CreatedAt = value.Time
			}
		case ratelimit.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				rl.UpdatedAt = value.Time
			}
		default:
			rl.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the RateLimit.
// This includes values selected through modifiers, order, etc.
func (rl *RateLimit) Value(name string) (ent.Value, error) {
	return rl.selectValues.Get(name)
}

// Update returns a builder for updating this RateLimit.
// Note that you need to call RateLimit.Unwrap() before calling this method if this RateLimit
// was returned from a transaction, and the transaction was committed or rolled back.
func (rl *RateLimit) Update() *RateLimitUpdateOne {
	return NewRateLimitClient(rl.config).UpdateOne(rl)
}

// Unwrap unwraps the RateLimit entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (rl *RateLimit) Unwrap() *RateLimit {
	_tx, ok := rl.config.driver.(*txDriver)
	if !ok {
		panic("ent: RateLimit is not a transactional entity")
	}
	rl.config.driver = _tx.drv
	return rl
}

// String implements the fmt.Stringer.
func (rl *RateLimit) String() string {
	var builder strings.Builder
	builder.WriteString("RateLimit(")
	builder.WriteString(fmt.Sprintf("id=%v, ", rl.ID))
	builder.WriteString("scope=")
	builder.WriteString(rl.Scope)
	builder.WriteString(", ")
	if v := rl.TenantID; v != nil {
		builder.WriteString("tenant_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := rl.DomainID; v != nil {
		builder.WriteString("domain_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	if v := rl.ServiceID; v != nil {
		builder.WriteString("service_id=")
		builder.WriteString(fmt.Sprintf("%v", *v))
	}
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(rl.Name)
	builder.WriteString(", ")
	builder.WriteString("limit=")
	builder.WriteString(fmt.Sprintf("%v", rl.Limit))
	builder.WriteString(", ")
	builder.WriteString("window_seconds=")
	builder.WriteString(fmt.Sprintf("%v", rl.WindowSeconds))
	builder.WriteString(", ")
	builder.WriteString("burst=")
	builder.WriteString(fmt.Sprintf("%v", rl.Burst))
	builder.WriteString(", ")
	builder.WriteString("response_code=")
	builder.WriteString(fmt.Sprintf("%v", rl.ResponseCode))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(rl.Status)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(rl.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(rl.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// RateLimits is a parsable slice of RateLimit.
type RateLimits []*RateLimit
